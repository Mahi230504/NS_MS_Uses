[pytest]
asyncio_mode = auto
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    live: requires real adb/emulator; runs only with MOBILE_AGENT_LIVE=1
